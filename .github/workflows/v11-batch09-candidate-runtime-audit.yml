name: v11 Batch09 candidate runtime audit
on:
  workflow_dispatch:
  push:
    branches: [v11-1000passage-easy-notes]
    paths:
      - 'v11_batch09_candidate_runtime_audit.js'
      - 'v11_batch09_question_human_rewrite.js'
      - 'v11_batch09_semantic_repair_*.js'
      - '.github/workflows/v11-batch09-candidate-runtime-audit.yml'
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v11-1000passage-easy-notes
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: |
          npm install --no-save --no-package-lock playwright@1.55.0
          npx playwright install --with-deps chromium webkit
      - run: |
          python3 -m http.server 8000 >/tmp/v11-b09-candidate-http.log 2>&1 &
          server=$!
          trap 'kill $server || true' EXIT
          sleep 2
          node v11_batch09_candidate_runtime_audit.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v11-batch09-candidate-runtime
          path: |
            V11_BATCH09_CANDIDATE_RUNTIME_AUDIT.json
            /tmp/v11-b09-candidate-a4/*.pdf
          if-no-files-found: warn
