name: v11 Batch06 final quality audit
on:
  workflow_dispatch:
  push:
    branches: [v11-1000passage-easy-notes]
    paths:
      - 'v11_batch06_*.js'
      - '.github/workflows/v11-batch06-final-quality-audit.yml'
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node v11_batch06_final_quality_audit.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v11-batch06-final-quality
          path: V11_BATCH06_FINAL_QUALITY_AUDIT.json
          if-no-files-found: error
